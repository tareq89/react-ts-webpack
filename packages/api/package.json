{
	"name": "@packages/api",
	"version": "1.0.0",
	"main": "lib",
	"license": "MIT",
	"scripts": {
		"build": "yarn clean:dist && tsc",
		"clean:dist": "rimraf lib",
		"clean:node_modules": "rimraf node_modules"
	},
	"dependencies": {
		"axios": "^0.19.2"
	},
	"devDependencies": {
		"rimraf": "^3.0.2",
		"typescript": "^3.9.7"
	}
}
